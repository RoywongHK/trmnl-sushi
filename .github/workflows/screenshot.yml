name: Auto Screenshot Sushi
on:
  schedule:
    - cron: '*/15 * * * *'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  screenshot:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Install Fonts
        run: |
          sudo apt-get update
          sudo apt-get install -y fonts-wqy-zenhei

      - name: Install Dependencies
        run: |
          npm init -y
          npm install puppeteer

      - name: Take Screenshot
        run: |
          node -e "
          const puppeteer = require('puppeteer');
          (async () => {
            const browser = await puppeteer.launch({ 
              headless: true,
              args: ['--no-sandbox', '--disable-setuid-sandbox']
            });
            const page = await browser.newPage();
            
            await page.setExtraHTTPHeaders({ 'Accept-Language': 'zh-HK,zh;q=0.9' });
            await page.setViewport({ width: 800, height: 480 });
            
            await page.goto('https://web.app', { waitUntil: 'networkidle0' });
            
            await new Promise(r => setTimeout(r, 6000));
            
            // 核心修正：移除了 grayscale(100%) 黑白濾鏡，改為輸出網頁原始彩色畫面
            // 只保留強制白色背景，確保截圖邊緣乾淨
            await page.addStyleTag({ content: 'body { background: #ffffff !important; }' });
            
            await page.screenshot({ path: 'sushi.png' });
            await browser.close();
          })();
          "

      - name: Commit and Push Image
        run: |
          git config --global user.name "GitHub Action"
          git config --global user.email "action@github.com"
          git add sushi.png
          git commit -m "Change screenshot to full color" || exit 0
          git push
